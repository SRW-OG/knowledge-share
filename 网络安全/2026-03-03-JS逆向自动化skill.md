---
title: JS逆向自动化 Skill - JSRPC+Flask+autoDecoder 方案
date: 2026-03-03
category: 网络安全
tags: [JS逆向, JSRPC, Flask, MCP, 自动化, Burp]
source: https://github.com/Fausto-404/js-reverse-automation--skill
---

# JS逆向自动化 Skill - JSRPC+Flask+autoDecoder 方案

## 一、问题现状：JS 逆向的痛点与挑战

### 背景描述

在前端安全测试中，**JS 逆向（JavaScript 逆向分析）** 是获取后端 API 签名/加密逻辑的关键技术。传统方法面临以下挑战：

- **定位加密函数困难**：需要手动在海量 JS 代码中追踪加密/签名逻辑
- **JSRPC 配置繁琐**：需要编写注册代码、编写 Python 后端、配置代理等
- **重复性工作**：每个目标都需要重复上述流程
- **与 Burp 联动困难**：难以实现端到端的自动化联调

### 风险评估

- **效率低下**：手动分析一个目标的加密逻辑可能需要数小时甚至数天
- **容易遗漏**：复杂的前端代码可能导致关键加密逻辑被忽略
- **反调试对抗**：越来越多的网站部署了反调试机制（Anti-Debug）

### 影响范围

- Web 安全测试工程师
- 渗透测试人员
- API 安全研究者

---

## 二、解决思路与方案对比

### 传统方案 vs 自动化方案

| 维度 | 传统 JSRPC 方案 | 本自动化方案 |
|------|-----------------|---------------|
| 函数定位 | 手动搜索 + 经验判断 | MCP 连接浏览器自动追踪 |
| 注册代码 | 手动编写 | AI 自动生成 |
| Python 后端 | 手动编写 Flask | AI 自动生成 |
| Burp 联动 | 手动配置 | autoDecoder 自动对接 |
| 反调试 | 手动绕过 | AntiDebug_Breaker 自动对抗 |
| 耗时 | 数小时 | 自动化执行，人工仅需输入目标信息 |

### 核心思路

**利用 AI 的 MCP（Model Context Protocol）和 Skill 技术，让 AI 自动完成：**
1. 连接真实浏览器，触发并跟踪 JS 加密链路
2. 定位 sign/enc/token 等关键参数
3. 生成 JSRPC 注入代码
4. 生成 Flask 代理代码
5. 输出 Burp autoDecoder 配置说明

---

## 三、最佳实践框架

### 系统架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   用户输入   │ ──> │   Codex/AI   │ ──> │  MCP 浏览器 │
│ (目标URL等)  │     │  (Skill执行)  │     │  (DevTools) │
└─────────────┘     └──────────────┘     └─────────────┘
                                                  │
                                                  ▼
                    ┌──────────────┐     ┌─────────────┐
                    │  Burp Suite  │ <── │   Flask    │
                    │ autoDecoder  │     │   代理     │
                    └──────────────┘     └─────────────┘
```

> **图1：整体流程架构图** - 展示 MCP + Skill + JSRPC + Flask + Burp 完整自动化流程

### 核心组件

| 组件 | 用途 |
|------|------|
| **chrome-devtools-mcp** | MCP 协议连接 Chrome DevTools |
| **JSRpc** | 远程调用 JS 函数 |
| **Flask** | Python 代理服务器 |
| **autoDecoder** | Burp 插件，自动解码/编码请求 |
| **AntiDebug_Breaker** | 11 项反调试对抗能力 |

### Skill 工作流程

```
用户输入目标信息
    │
    ▼
1. MCP 连接浏览器，触发加密请求
    │
    ▼
2. 自动追踪 JS 执行链路，定位加密函数
    │
    ▼
3. 生成 JSRPC 注入代码 (jsrpc_inject_xxx.js)
    │
    ▼
4. 生成 Flask 代理代码 (flask_proxy_xxx.py)
    │
    ▼
5. 输出 Burp autoDecoder 配置说明
    │
    ▼
最终输出：可复用的加密/签名解决方案
```

---

## 四、具体操作步骤

### 前置条件

1. **安装 Chrome 扩展**（如果是 Codex/Claude 等平台）
2. **配置 chrome-devtools-mcp**
3. **启动 Chrome 远程调试模式**

### Step 1：配置 MCP 环境

**1.1 下载 Skills**

将下载的 skills 放置到平台的 skills 目录：
- Codex: `~/.codex/skills/`
- 目录结构：`js-reverse-automation--skill/`

> **图2：Skills 目录结构** - 展示 skills 文件夹在平台中的放置位置

---

**1.2 添加 chrome-devtools-mcp 服务**

```bash
# Codex 示例
codex mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest
```

> **图3：codex mcp add 命令** - 终端执行添加 MCP 服务的命令

---

**1.3 修改配置文件**

修改平台的配置文件 (~/.codex/config.toml)：

```toml
[mcp_servers.chrome-devtools]
command = "npx"
args = ["-y", "chrome-devtools-mcp@latest"]
```

> **图4：config.toml 配置示例** - 展示完整的 MCP 服务器配置内容

---

**1.4 检测 MCP 服务是否生效**

> **图5：MCP 服务检测** - 验证 MCP 是否成功连接，显示已连接的浏览器目标

---

**1.5 启动 Chrome 远程调试**

```bash
# macOS
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
    --remote-debugging-port=9222 \
    --remote-debugging-address=0.0.0.0

# Linux
google-chrome --remote-debugging-port=9222 --remote-debugging-address=0.0.0.0
```

> **图6：Chrome 启动命令** - 终端显示 Chrome 远程调试模式启动命令及参数

---

### Step 2：使用 Skill

**2.1 在 AI 客户端中选择 Skill**

> **图7：Codex 中使用 Skill** - 展示 AI 客户端中选择 js-reverse-automation Skill 的界面

---

**2.2 输入目标信息**

需要输入以下 4 项信息：

```
1、目标网址（完整 URL）：
2、需要分析的加密参数名（如 sign / enc / token）：
3、可复现请求示例（优先给 fetch/抓包原始请求）：
4、环境限制（浏览器版本、是否需要代理/插件、是否允许注入）：
```

> **图8：输入目标信息** - 展示 Skill 运行时要求用户输入的 4 个必填项

---

**2.3 等待自动化执行**

> **图9：自动化执行中** - 展示 Skill 运行时 AI 正在自动分析、生成代码的过程界面

---

### Step 3：验证与联调

**3.1 启动 JSRPC 服务**

```bash
# 启动 JSRPC 监听
python jsrpc_server.py
```

> **图10：JSRPC 启动** - 展示 JSRPC 服务端启动成功，监听端口 12080 的界面

---

**3.2 注入 JS 环境**

在浏览器 DevTools Console 中执行：
1. 首先执行 `JsEnv_Dev.js`（JSRPC 环境初始化）
2. 然后执行 AI 生成的 `jsrpc_inject_xxx.js`

> **图11：JsEnv_Dev.js 执行** - 展示在浏览器开发者工具 Console 中执行初始化 JS 的界面

---

> **图12：注入 AI 生成的 JS** - 展示在 Console 中注入 AI 生成的 jsrpc_inject_xxx.js 的界面

---

**3.3 测试 JSRPC 调用**

```bash
curl "http://127.0.0.1:12080/go?group=fausto&action=generate_sign&param=test"
```

> **图13：JSRPC 调用测试** - 展示 curl 请求及返回的加密结果

---

**3.4 启动 Flask 代理**

```bash
python flask_proxy_xxx.py
```

> **图14：Flask 代理启动** - 展示 Flask 服务启动，监听 8888 端口的界面

---

**3.5 测试 Flask 加密**

```bash
curl -X POST http://127.0.0.1:8888/encode \
  -H "Content-Type: application/x-www-form-urlencoded" \
  --data-urlencode "dataBody=username=test&password=123456"
```

> **图15：Flask 加密测试** - 展示 curl POST 请求及返回的加密结果

---

**3.6 配置 Burp autoDecoder**

根据 AI 输出的配置说明，在 Burp Suite 中配置 autoDecoder 插件。

> **图16：Burp autoDecoder 配置** - 展示 Burp 插件配置界面，设置编码/解码规则

---

**3.7 端到端联调成功**

> **图17：端到端联调成功** - 展示从浏览器触发请求 → JSRPC → Flask → Burp 全流程联调成功的最终效果

---

## 五、总结与建议

### 核心价值

| 能力 | 描述 |
|------|------|
| **自动化程度高** | 从函数定位到代码生成全流程自动化 |
| **端到端联调** | 完整支持 JSRPC → Flask → Burp 联动 |
| **反调试对抗** | 内置 11 项 AntiDebug 能力 |
| **可复用性强** | 生成的代码可保存复用 |

### 适用场景

- ✅ 快速落地前端签名/加密参数逆向
- ✅ 将 JS 逆向逻辑封装为可复用代码
- ✅ 与 Burp 配合进行抓包、改包测试

### 局限性

- ⚠️ 需要目标站点允许浏览器调试（无 CSP 严格限制）
- ⚠️ 某些高级反调试可能需要额外对抗
- ⚠️ 依赖 MCP 服务的稳定性

### 引用工具

- [JsRpc](https://github.com/jxhczhl/JsRpc)：JS 远程调用框架
- [autoDecoder](https://github.com/f0ng/autoDecoder)：Burp 自动解码插件
- [chrome-devtools-mcp](https://github.com/ChromeDevTools/chrome-devtools-mcp/)：Chrome DevTools MCP 服务
- [AntiDebug_Breaker](https://github.com/0xsdeo/AntiDebug_Breaker)：反调试对抗工具

---

> 参考来源：https://github.com/Fausto-404/js-reverse-automation--skill
> 首发：【AI赋能】MCP+Skill能力下的前端JS逆向自动化落地 - 先知社区
